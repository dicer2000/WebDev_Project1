// settings.js

export const settings = {
    seaweedSpawnInterval: 500,  // in ms, spawn new seaweed every 1 second
    seaweedSpeed: 0.02,          // how fast seaweed moves toward the camera
    farLimit: 90,                // remove seaweed after it passes this z-limit
    waveAmplitude: 0.3,          // amplitude of the wave in the X direction
    waveSpeed: 2.5,              // speed of the sine wave
    segmentCount: 6              // how many segments each seaweed line has
  };
  